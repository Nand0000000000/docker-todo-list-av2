apiVersion: v1
kind: Service
metadata:
  name: todo-app
  namespace: default
  labels:
    app: todo-app
spec:
  type: LoadBalancer
  selector:
    app: todo-app
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
